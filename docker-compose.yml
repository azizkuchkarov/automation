services:
  postgres:
    image: postgres:16-alpine
    restart: unless-stopped
    environment:
      POSTGRES_DB: automation
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-postgres}
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres -d automation"]
      interval: 5s
      timeout: 5s
      retries: 12
      start_period: 10s
    ports:
      - "${POSTGRES_PORT:-5432}:5432"

  minio:
    image: minio/minio:latest
    restart: unless-stopped
    command: server /data --console-address ":9001"
    environment:
      MINIO_ROOT_USER: ${MINIO_ROOT_USER:-minioadmin}
      MINIO_ROOT_PASSWORD: ${MINIO_ROOT_PASSWORD:-minioadmin}
    volumes:
      - minio_data:/data
    ports:
      - "${MINIO_PORT:-9000}:9000"
      - "${MINIO_CONSOLE_PORT:-9001}:9001"

  e-imzo-server:
    build:
      context: ./e-imzo-server
    restart: unless-stopped
    ports:
      - "${EIMZO_SERVER_PORT:-18080}:8080"

  api:
    build:
      context: .
      dockerfile: ATG.Platform.API/Dockerfile
    restart: unless-stopped
    depends_on:
      postgres:
        condition: service_healthy
      minio:
        condition: service_started
      e-imzo-server:
        condition: service_started
    environment:
      ASPNETCORE_ENVIRONMENT: ${ASPNETCORE_ENVIRONMENT:-Development}
      ASPNETCORE_URLS: http://+:8080
      ConnectionStrings__Default: Host=postgres;Port=5432;Database=automation;Username=postgres;Password=${POSTGRES_PASSWORD:-postgres}
      Jwt__SecretKey: ${JWT_SECRET:-atg-platform-secret-key-min-32-chars-long}
      Jwt__Issuer: atg-platform
      Jwt__Audience: atg-users
      Frontend__Url: ${FRONTEND_URL:-http://localhost:3000}
      Ldap__Enabled: "false"
      Minio__Enabled: "true"
      Minio__Endpoint: minio:9000
      Minio__AccessKey: ${MINIO_ROOT_USER:-minioadmin}
      Minio__SecretKey: ${MINIO_ROOT_PASSWORD:-minioadmin}
      Minio__Bucket: atg-marketing
      Minio__UseSsl: "false"
      Hangfire__Enabled: "true"
      Eimzo__Enabled: "true"
      Eimzo__ServerBaseUrl: http://e-imzo-server:8080
      Eimzo__SiteHost: ${EIMZO_SITE_HOST:-unilogin.atg.uz}
      Eimzo__FrontendApiKey: ${EIMZO_FRONTEND_API_KEY:-}
      Eimzo__ClientIp: ${EIMZO_CLIENT_IP:-}
      Eimzo__AllowUnsignedPkcs7: ${EIMZO_ALLOW_UNSIGNED_PKCS7:-true}
      HrLeave__ShortHoApprovalChain: ${HR_LEAVE_SHORT_HO_CHAIN:-true}
      HrLeave__HoGeneralDirectorEmail: ${HR_LEAVE_HO_GD_EMAIL:-liuzhiguang@atg.uz}
      HrLeave__PublicAppBaseUrl: ${HR_LEAVE_PUBLIC_APP_URL:-http://localhost:3000}
    volumes:
      - api_uploads:/app/uploads
    ports:
      - "${API_PORT:-5161}:8080"

  frontend:
    build:
      context: ./atg-platform
      dockerfile: Dockerfile
      args:
        INTERNAL_API_URL: http://api:8080
        NEXT_PUBLIC_API_URL: ${NEXT_PUBLIC_API_URL:-http://localhost:5161}
        NEXT_PUBLIC_EIMZO_DOMAIN: ${NEXT_PUBLIC_EIMZO_DOMAIN:-unilogin.atg.uz}
        NEXT_PUBLIC_EIMZO_API_KEY: ${NEXT_PUBLIC_EIMZO_API_KEY:-}
    restart: unless-stopped
    depends_on:
      - api
    environment:
      INTERNAL_API_URL: http://api:8080
    ports:
      - "${FRONTEND_PORT:-3000}:3000"

volumes:
  postgres_data:
  minio_data:
  api_uploads:
